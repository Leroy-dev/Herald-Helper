<UserControl x:Class="HeraldHelper.Desktop.Views.LiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource VsWindowBackgroundBrush}">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <GroupBox Grid.Row="0" Header="Status">
                            <TextBox x:Name="OutputBox" x:FieldModifier="public" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     Background="{DynamicResource VsPanelAltBrush}" Foreground="{DynamicResource VsTextBrush}" BorderBrush="{DynamicResource VsBorderBrush}" />
                        </GroupBox>
                        <GroupBox Grid.Row="1" Header="OCR Diagnostics">
                            <TextBox x:Name="DiagnosticsBox" x:FieldModifier="public" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     Background="{DynamicResource VsPanelAltBrush}" Foreground="{DynamicResource VsTextBrush}" BorderBrush="{DynamicResource VsBorderBrush}" />
                        </GroupBox>
                        <GroupBox Grid.Row="2" Header="Response Diagnostics">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <WrapPanel Margin="0,0,0,6">
                                    <Button Style="{StaticResource ToolbarButtonStyle}" Content="Clear Log" Click="ClearResponseDiagnostics_Click" />
                                </WrapPanel>
                                <TextBox Grid.Row="1"
                                         x:Name="ResponseDiagnosticsBox" x:FieldModifier="public"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         Background="{DynamicResource VsPanelAltBrush}"
                                         Foreground="{DynamicResource VsTextBrush}"
                                         BorderBrush="{DynamicResource VsBorderBrush}" />
                            </Grid>
                        </GroupBox>
                    </Grid>

</UserControl>
